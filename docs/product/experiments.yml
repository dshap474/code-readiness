experiments:
  - name: widget_write_enabled
    status: active
    audience: Operators controlling write availability during incidents or staged rollouts
    success_metric: Widget write traffic can be paused without impacting read-only flows
    guardrail: Leave enabled by default and disable only with an explicit incident or rollout plan
    feature_flag: widget_write_enabled
    rollout_strategy:
      type: percentage
      default_percentage: 100
      config_key: widget_write_enabled_rollout
